
<%= simple_form_for([@boat,@booking])  do |f|  %>
  <h3 class="pb-4">Create your booking 👇🏻</h3>
    <div class="form-inputs">
        <%= f.input :check_in %>
        <%= f.input :check_out %>
        <%= f.button :submit, class: 'btn btn-success' %>
      </div>
  <% end %>
